services:
  wb-max-bot:
    build: .
    container_name: wb-max-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./certs:/app/certs:ro
    ports:
      - "127.0.0.1:${APP_HOST_PORT:-18080}:8080"
