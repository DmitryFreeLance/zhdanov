services:
  wb-max-bot:
    build: .
    container_name: wb-max-bot
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./certs:/app/certs:ro
    restart: unless-stopped
